# backend/app/attendance/evaluator.py

def evaluate_presence(student_id: str) -> bool:
    """
    Demo attendance evaluator.
    Always returns True for demo.
    (Later: BLE + duration + anomalies)
    """
    return True
